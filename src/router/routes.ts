export const routes = {
  home: "/",
  organizations: "/organizations",
  organization: "/organizations/:id",
  testReports: "/organizations/:id/reports",
  reportDetails: "/organizations/:id/reports/:reportId"
}
